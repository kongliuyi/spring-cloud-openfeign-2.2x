server:
  port: ${SERVER_PORT:8011}
spring:
  application:
    name: openfeign-consumer
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:192.168.235.3}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:192.168.235.3}:${REGISTER_PORT:8848}
        file-extension: yaml

